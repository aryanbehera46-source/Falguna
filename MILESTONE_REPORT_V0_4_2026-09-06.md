# FALGUNA BOOTSTRAP v0.4 REAL-PROJECT TRIAL COMPLETE

Date: 2026-09-06 (Asia/Kolkata)

## Decision

Bootstrap v0.4 passed on real-project run `9060c767-33a8-4dd3-ba3d-f0a6f7aadf13`. It reached `DONE_CANDIDATE`; protected-main approval `676f366a-feb0-41c8-8c9c-48aa7fd047c8` remains `PENDING`. No merge or deployment occurred.

Falguna is now usable internally for bounded engineering work with explicit requirements, isolated worktrees, locally reproducible verification, no production secrets, and human merge approval. It is not approved for hostile code, unattended deployment, or broader Company OS operations.

## Chosen repository and genuine defect proof

- Repository: ServiceFlow, an established booking/CRM application.
- Frozen production commit: `615b645ecbd3c505293f4f77a2576c8dca97f2d6`.
- Before the trial, a black-box request rescheduled a provider working `09:00–18:00` to `03:17`; the authenticated API returned `200` and persisted the invalid value.
- Static cause: appointment creation enforced provider hours and 30-minute alignment, while authenticated appointment updates checked overlap only.
- The production checkout remained clean and unchanged at `615b645`.

## Requirement and candidate

The requirement map was frozen before implementation. The worker could edit only:

- `server.js`
- `tests/workflow.test.js`

The model-authored candidate adds the missing non-cancelled update boundaries, retains overlap checking, moves the native test database inside the isolated worktree for Seatbelt compatibility, and adds focused coverage for before-hours, off-grid, beyond-end, unchanged persistence, and a valid update.

No Aryan/manual candidate-solution code was written. Candidate diff SHA-256: `7944fa0f3c7dc7378960553a05a41e25ee2f06764e48e2e4cbe898ad112f34ff`.

## Verification

- Native ServiceFlow suite: 7/7 passed.
- Native containment: macOS Seatbelt; writes limited to the worktree/private temporary home; localhost networking allowed for the project's HTTP tests; external networking denied.
- Browser: Playwright 1.62.1 with cached Chromium revision 1234; one real login/admin UI + API acceptance test passed.
- Invalid `03:17` update: rejected; UI showed the working-hours error; stored appointment remained unchanged.
- Browser boundary marker: `external_blocked=true localhost_ok=true`.
- Browser enforcement remains Playwright runtime-level, not kernel/VM-grade.
- Prohibited write probe: blocked with `Operation not permitted`.
- Test database cleanup: confirmed; no `.serviceflow-test-*.db*` leftovers.
- Candidate worktree after cleanup: only the two authorized modified files.

Verification artifact SHA-256: `4989b019d12db8d0018eb5c87cb489dc23a3202022c64685002e370a5be3b582`.

## Independent review and evidence

- Calibration: 3/3 correct, 0 false accepts, 0 false rejects.
- Final review: approved across requirement satisfaction, scope compliance, regression evidence, and unresolved uncertainty.
- Blocking findings: 0.
- Unresolved uncertainty: 0.
- Retained artifacts: 8/8 hashes valid.
- Audit chain: valid.
- Security violations in the accepted run: 0.
- Protected-main merges: 0.

Final review SHA-256: `e57858a828d7ef2994df27c12d1cd54064cd1583255a82ece789f5853b78f0e4`.

## Intervention and calibration history

Acceptance intervention: `H1`, infrastructure-only. Real-project native tests require a localhost HTTP listener; Falguna was extended so a verification command can request localhost-only networking while retaining Seatbelt filesystem containment. The timeout handler was also corrected to preserve bytes/text output safely. Falguna's suite passed 21/21 after this change.

Pre-acceptance attempts failed closed for transparent harness reasons: `/tmp` database writes were blocked by Seatbelt, generated Playwright output exceeded the changed-file gate, one combined model call timed out, and one split test patch returned an absolute path and was rejected. No failed candidate was merged, deployed, or copied into the accepted candidate. The accepted candidate was generated by the worker and resumed from its preserved `WORKER_COMPLETE`/`VERIFIED` checkpoints after infrastructure evidence was completed.

## Cost

- Model: `gpt-5.4-mini`; no upgrade.
- Actual additional cash cost: `$0.00` through the authenticated local subscription transport.
- Reset credit: untouched.
- Recorded accepted-run usage including calibration/review retries: 248,886 input tokens, 132,096 cached input tokens, and 27,052 output tokens.
- Standard API list-price equivalent at the verified rates of $0.75/M input, $0.075/M cached input, and $4.50/M output: `$0.21923370`.

The notional equivalent exceeded the initial $0.08 planning estimate because fail-closed review evidence was rerun; it was not billed API spend. No materially paid run occurred. Future operational runs should cap reviewer retries separately to prevent this notional overrun.

## Intel Mac limitations

Seatbelt is deprecated and is not a namespace, cgroup, container, microVM, or VM. Native commands receive Seatbelt containment, including localhost-only network rules where explicitly required. Chromium remains outside Seatbelt because it cannot reliably start there; browser requests are controlled by the Playwright runtime allowlist plus process timeout/resource limits. Do not use this setup for hostile untrusted workloads or production secrets.

## Smallest next stage

Bootstrap v0.5: command/status/approval usability only—make mission launch, progress visibility, evidence summary, failure classification, and the human merge-decision surface operationally simple. Do not start it without Aryan's approval.
